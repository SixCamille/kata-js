# Ressources utiles

```js
butterfly.classList.add("is-flying");
```

```js
setTimeout(function () {
  butterfly.classList.remove("is-flying");
}, 3000);
```

Une animation CSS peut modifier `transform` entre `from` et `to`.
