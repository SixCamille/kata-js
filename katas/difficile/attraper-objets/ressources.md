# Ressources utiles

```js
stage.addEventListener("mousemove", function (event) {
  basket.style.left = event.clientX + "px";
});
```

`getBoundingClientRect()` peut aider a comparer la position du panier et celle d'un objet.
