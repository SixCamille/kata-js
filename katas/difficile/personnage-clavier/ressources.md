# Ressources utiles

```js
document.addEventListener("keydown", function (event) {
  console.log(event.key);
});
```

Les touches utiles sont souvent `"ArrowUp"`, `"ArrowDown"`, `"ArrowLeft"` et `"ArrowRight"`.
