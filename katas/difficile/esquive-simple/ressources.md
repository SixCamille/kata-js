# Ressources utiles

```js
const playerRect = player.getBoundingClientRect();
const obstacleRect = obstacle.getBoundingClientRect();
```

Deux rectangles se touchent si leurs zones se recouvrent horizontalement et verticalement.
