# Ressources utiles

```js
const card = document.querySelector(".card");

card.addEventListener("click", function () {
  card.classList.toggle("is-flipped");
});
```

La classe ajoutee par JavaScript peut ensuite etre utilisee dans le CSS.
