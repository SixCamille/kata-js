# Ressources utiles

```js
const modal = document.querySelector(".modal");
modal.classList.add("is-open");
modal.classList.remove("is-open");
```

Une meme classe peut afficher a la fois le fond sombre et la boite de dialogue.
